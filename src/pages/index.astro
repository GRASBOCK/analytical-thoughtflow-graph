---
import Layout from '../layouts/Layout.astro';
import VizNetwork from '../components/VizNetwork.svelte';
import { Graph } from '../graph';

import graph_file from '../../public/graph.json'

let g : Graph = Object.assign( new Graph(), graph_file as Object );
console.log(g)
---

<Layout title="Research Graph">
	<VizNetwork graph={g} client:load></VizNetwork>
</Layout>